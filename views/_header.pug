header.header
  nav.nav.nav--tours
    a.nav__el(href='/') All tours
  .header__logo
    img(src='/img/logo-white.png' alt='Natours logo')
  nav.nav.nav--user
    if user
      a.nav__el.nav__el--logout(href='/logout') Log out
      a.nav__el(href='/me')
        img.nav__user-img(src=user.photoUrl alt=`Photo of ${user.name}`)
        span= user.name.split(' ')[0]
      // Додати кнопку для неподтвердженого email
      if !user.emailConfirmed
        a.nav__el.nav__el--resend#resend-email-btn(href='#' title='Click to resend confirmation email')
          | ⚠️ Confirm Email
    else
      a.nav__el(href='/login') Log in
      a.nav__el.nav__el--cta(href='/signup') Sign up